# API v1 root (без завершающего /)
# Локально (MSW): /api/v1 — суффиксы /bean/request, /auth/token, /logout в коде
# Прод / CMS: https://music.mastermindcms.com/api/v1
VITE_API_BASE_URL=/api/v1

# App base path: / or /music-store-project/ for example
VITE_APP_BASE_URL=/

# MSW worker in browser (local mocks for bean + auth)
VITE_ENABLE_MSW=true
